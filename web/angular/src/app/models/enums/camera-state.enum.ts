/**
 * Enumeration of all the current camera states
 */
export enum CameraStateEnum {
    /**
     * The camera is idle
     */
    Stopped = 1,

    /**
     * The camera is recording
     */
    Recording = 2
}
